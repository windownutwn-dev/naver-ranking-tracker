[build]
builder = "dockerfile"

[deploy]
startCommand = "npx prisma db push --accept-data-loss; npx tsx scripts/deploy-init.ts; node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
