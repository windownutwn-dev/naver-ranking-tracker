[build]
builder = "dockerfile"

[deploy]
startCommand = "npx prisma db push --accept-data-loss; npx tsx scripts/deploy-init.ts; npx next start -p ${PORT:-3000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
